{
  "enabled": {
    "name": "启用插件",
    "type": "switch",
    "default": true,
    "hint": "是否启用标签修复功能"
  },
  "only_final_message": {
    "name": "仅处理最终消息",
    "type": "switch",
    "default": false,
    "hint": "开启后只处理没有 tool_calls 的最终回复。建议保持关闭：工具调用是结构化字段不受修复影响，而中间步骤的旁白同样会被发送并写入记忆，不修复会让坏格式污染记忆。关了还能看ta跟你说点过程中的消息"
  },
  "fix_missing_msg": {
    "name": "补全缺失的msg标签",
    "type": "switch",
    "default": true,
    "hint": "如果没有 <msg> 根标签，自动添加"
  },
  "fix_double_brackets": {
    "name": "修复双尖括号",
    "type": "switch",
    "default": true,
    "hint": "将 <<msg 修复为 <msg, <<function_calls 修复为 <function_calls 等"
  },
  "escape_special_chars": {
    "name": "转义裸特殊字符",
    "type": "switch",
    "default": true,
    "hint": "将文本中未转义的 &（非实体）和 <（非标签开头）转义为合法 XML。代码、payload、URL 参数等内容不开启时会解析失败导致消息发不出"
  },
  "fallback_wrap_text": {
    "name": "终极兜底为纯文本",
    "type": "switch",
    "default": true,
    "hint": "所有修复手段都失败时，剥离结构性标签、只留文本内容包成 <msg><text> 发出，保证消息不丢、进入记忆的永远是良构格式"
  },
  "fallback_strip_tags": {
    "name": "兜底时剥离结构性标签",
    "type": "switch",
    "default": true,
    "hint": "开启：兜底只保留干净文本，用户看不到 <msg>/<text> 原文，代码围栏内的内容逐字保留（适合日常聊天）。关闭：兜底整段转义、逐字保真（适合 payload/注入测试等要求内容原样不变的场景）"
  },
  "fix_at_tag_format": {
    "name": "修复 At 标签格式",
    "type": "switch",
    "default": true,
    "hint": "将 <at user_id=\"123\" /> 修复为 <at>123</at>，并处理尾随文本"
  },
  "no_wrap_tags": {
    "type": "list",
    "default": ["mimo_tts"],
    "hint": "不包裹 <text> 的自定义标签名单，填纯标签名即可（如 mimo_tts），带不带尖括号都能识别。mimo_tts 始终内置保护，可自行追加其他插件的标签"
  },
  "flatten_no_wrap_tags": {
    "name": "摊平不包裹标签内的嵌套",
    "type": "switch",
    "default": true,
    "hint": "剥掉上面名单内标签被模型错误嵌套的 <text> 等子标签（如 <mimo_tts><text>内容</text></mimo_tts> → <mimo_tts>内容</mimo_tts>），防止内容在解析阶段静默丢失"
  },
  "fix_record_split": {
    "name": "语音消息拆分发送",
    "type": "switch",
    "default": true,
    "hint": "把混在消息里的语音（官方 <record> 与 <mimo_tts> 均生效）拆成单条干净消息发送，@/回复迁移到文字消息，避免 QQ 上语音无法正常显示。检测到 MiMo TTS 插件时会自动接管其格式修复"
  },
  "split_blank_line_messages": {
    "name": "空行分段拆为多条消息",
    "type": "switch",
    "default": false,
    "hint": "开启后，单条消息内的空行分段会拆成多条消息发送（模拟真人分段说话）。含代码围栏或 [xxx] 式文本标记（如折扇留穗的 [3p]）的消息整条不拆；仅对纯文本消息生效。可能改变说话风格，根据情况自行开启"
  },
  "strip_reasoning_block": {
    "name": "剥离内联 reasoning 思考块",
    "type": "switch",
    "default": true,
    "hint": "部分渠道/模型会把思考过程以 <reasoning> 标签写进正文（而不是 reasoning_content 字段），其中常含消息草稿和示例，不剥离会泄漏给用户。建议保持开启"
  },
  "merge_marker_span_msgs": {
    "name": "合并跨消息的 [xxx] 标记对",
    "type": "switch",
    "default": true,
    "hint": "当 [xxx] 与 [/xxx] 被 AI 分散到多条消息时，自动把横跨的纯文本消息合并为一条，让折扇留穗等依赖单条消息内匹配标记对的插件能正常识别"
  },
  "convert_text_at_to_tag": {
    "name": "将文本 @数字 转换为 at 标签",
    "type": "switch",
    "default": false,
    "hint": "开启后，会将 text 节点中的 @纯数字（至少4位）替换为 <at>数字</at>。注意：虽已排除了常见出错情况，但此项仍处于实验性阶段，建议AI经常出现@数字的非有效方式时才启用"
  },
  "text_at_exclude_domains": {
    "type": "list",
    "default": ["com", "cn", "net", "org", "edu", "gov", "io", "co", "uk", "jp", "de", "fr", "ru"],
    "hint": "排除的邮箱域名后缀列表，当 @数字 后面紧跟 .后缀 时不触发 at 转换。可自行添加或删除"
  }
}
